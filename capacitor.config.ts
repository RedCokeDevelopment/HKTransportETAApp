import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'HK Transport Arrival Estimation',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
