import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.redcoke.hktransporteta',
  appName: 'HK Transport Arrival Estimation',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
